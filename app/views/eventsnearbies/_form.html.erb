<%= form_with(model: eventsnearby, local: true) do |form| %>
  <% if eventsnearby.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(eventsnearby.errors.count, "error") %> prohibited this eventsnearby from being saved:</h2>

      <ul>
      <% eventsnearby.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
