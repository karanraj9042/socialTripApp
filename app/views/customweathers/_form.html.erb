<%= form_with(model: customweather, local: true) do |form| %>
  <% if customweather.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(customweather.errors.count, "error") %> prohibited this customweather from being saved:</h2>

      <ul>
      <% customweather.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
